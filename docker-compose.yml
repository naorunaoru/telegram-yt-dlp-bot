services:
  media-grabber-bot:
    build: .
    env_file:
      - .env
    # Uncomment for VAAPI devices
    # devices:
    #   - /dev/dri:/dev/dri
    # Uncomment for NVIDIA GPUs
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [compute, utility, video]
    environment:
      - HW_ACCEL=none
      - MAX_RESOLUTION=720
      - VERBOSE_GROUPS=false
    # Mount exported browser cookies here and set:
    # DOWNLOADER_COOKIES_FILE=/run/secrets/downloader-cookies.txt
    # volumes:
    #   - ./secrets/downloader-cookies.txt:/run/secrets/downloader-cookies.txt:ro
